$("#comment_body").val('');
$("#comments").html('<%= escape_javascript(render partial: "comments/comment", collection: @relative.comments) %>');
$("#comments").animate({ 'scrollTop': $("#comments")[0].scrollHeight }, 300);