
<div id="view_style">
  <b>Choose style from below</b>
      <% if @tags %>
        <div>
          <div class="wrapper-tags">
              <% tag_cloud(@tags, %w(tag-size-1 tag-size-2 tag-size-3 tag-size-4)) do |tag, css_class| %>
                <%= link_to tag.name, "#", class: css_class, data: { tag: tag.name } %>
              <% end %>
              <div id="unselect-all">Unselect all</div>
          </div>
        </div>
      <% end %>
      <div class="" id="styles">
        <%= render partial: "show_styles", locals: {styles: @styles} %>
      </div>
  <%= hidden_field_tag 'queue_image[view_style]', view_style %>
</div>
